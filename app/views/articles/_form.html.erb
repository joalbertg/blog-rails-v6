<%= form_with(model: @article, local: true) do |f| %>
  <div class='field'>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class='field'>
    <%= f.label :content %>
    <%= f.rich_text_area :content %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
